{
  "name": "classic-snake-online",
  "version": "1.0.0",
  "description": "Classic Snake game with TypeScript and ADT design",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:client": "pnpm --filter @snake/web-ui dev",
    "dev:server": "pnpm --filter @snake/server dev",
    "preview:client": "pnpm --filter @snake/web-ui preview"
  },
  "keywords": ["snake", "game", "typescript", "canvas"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
